# สรุปการเปลี่ยนแปลง - Multi-Company Support

## 🎯 สิ่งที่เปลี่ยนแปลง

แอป HR ได้รับการอัพเกรดให้รองรับการทำงานหลายบริษัท โดยระบบจะค้นหาบริษัทที่ผู้ใช้สังกัดอัตโนมัติผ่าน LINE User ID

---

## ✨ ฟีเจอร์ใหม่

### 1. ระบบค้นหาบริษัทอัตโนมัติ
- ใช้ API: `https://search.user.dataslot.app/indexes/users/search`
- ค้นหาด้วย LINE User ID (`lUId`)
- แสดงบริษัททั้งหมดที่ผู้ใช้มีสิทธิ์เข้าถึง

### 2. หน้าเลือกบริษัท (Company Selection)
- แสดงเมื่อผู้ใช้มีหลายบริษัท
- แสดงข้อมูล: ชื่อบริษัท, สถานะ, บทบาท, รหัสพนักงาน
- เลือกอัตโนมัติถ้ามีแค่ 1 บริษัท

### 3. API แบบ Dynamic
- ทุก API ใช้ชื่อบริษัทแบบ dynamic
- รูปแบบ: `https://api.dataslot.app/wfm/{companyName}/tasks`
- เปลี่ยนบริษัทได้โดยไม่ต้อง restart

### 4. แสดงบริษัทปัจจุบันในหน้า Profile
- ผู้ใช้สามารถดูว่ากำลังใช้งานบริษัทไหนอยู่

---

## 📁 ไฟล์ที่สร้างใหม่

### 1. `services/companyService.ts`
จัดการข้อมูลบริษัทและการเลือกบริษัท

**ฟังก์ชันหลัก:**
- `fetchUserCompanies()` - ดึงข้อมูลบริษัททั้งหมด
- `getUniqueCompanies()` - กรองบริษัทที่ซ้ำ
- `storeSelectedCompany()` - บันทึกบริษัทที่เลือก
- `getSelectedCompany()` - ดึงบริษัทที่เลือก
- `getCompanyNameForAPI()` - ดึงชื่อบริษัทสำหรับ API

### 2. `app/company-selection.tsx`
หน้าจอเลือกบริษัท (สำหรับผู้ใช้ที่มีหลายบริษัท)

---

## 🔄 ไฟล์ที่แก้ไข

### 1. `services/lineAuth.ts`
- เปลี่ยน API endpoint จาก hardcode เป็น dynamic
- เพิ่ม parameter `companyName` ใน `checkUserAuthorization()`
- ล้างข้อมูล company เมื่อ logout

### 2. `services/clockService.ts`
- เปลี่ยน API endpoint เป็น dynamic
- ดึงชื่อบริษัทจาก storage ก่อนเรียก API
- ใช้ `getCompanyNameForAPI()` ในทุกฟังก์ชัน

### 3. `app/login.tsx`
- เพิ่มขั้นตอนค้นหาบริษัทหลัง LINE Login
- จัดการ flow สำหรับ 1 บริษัท vs หลายบริษัท
- บันทึกข้อมูลชั่วคราวสำหรับหน้าเลือกบริษัท

### 4. `app/(tabs)/profile.tsx`
- แสดงชื่อบริษัทปัจจุบัน
- โหลดข้อมูลบริษัทเมื่อเปิดหน้า

### 5. `README.md`
- อัพเดทคู่มือการใช้งาน
- เพิ่มส่วน Multi-Company Architecture
- เพิ่มตัวอย่าง API endpoints

---

## 🔄 Flow การทำงานใหม่

### เข้าสู่ระบบ (Login)

```
1. กด "Sign in with LINE"
   ↓
2. ยืนยันตัวตนผ่าน LINE
   ↓
3. ระบบค้นหาบริษัทจาก LINE User ID
   ↓
4. ตรวจสอบจำนวนบริษัท
   ├─ 1 บริษัท → เลือกอัตโนมัติ → เข้าสู่แอป
   └─ หลายบริษัท → แสดงหน้าเลือกบริษัท
                    ↓
                  ผู้ใช้เลือกบริษัท
                    ↓
                  เข้าสู่แอป
```

### Clock In/Out

```
1. กด "Clock In"
   ↓
2. ระบบดึงชื่อบริษัทจาก storage
   ↓
3. สร้าง URL: /wfm/{companyName}/tasks
   ↓
4. เรียก API
   ↓
5. บันทึกเวลาสำเร็จ
```

---

## 🗄️ ข้อมูลที่เก็บใน AsyncStorage

| Key | คำอธิบาย | ลบเมื่อ Logout |
|-----|----------|----------------|
| `selectedCompany` | บริษัทที่เลือกปัจจุบัน | ✅ ใช่ |
| `tempCompanies` | บริษัททั้งหมด (ชั่วคราว) | ✅ ใช่ |
| `tempLineProfile` | ข้อมูล LINE (ชั่วคราว) | ✅ ใช่ |
| `lineUserInfo` | ข้อมูล user และ employee | ✅ ใช่ |
| `clockRecords` | บันทึกเวลาทำงาน (local) | ❌ ไม่ |

---

## 🔌 API ที่ใช้

### 1. User Search API (ค้นหาบริษัท)
```
POST https://search.user.dataslot.app/indexes/users/search
Authorization: Bearer OGU5Yjk0NDY4MTRjMmRjMWZkZTc0OWZi
```

**Request:**
```json
{
  "limit": 10,
  "filter": ["lUId = {LINE_USER_ID}"],
  "sort": []
}
```

**Response:**
```json
{
  "hits": [
    {
      "company": "ATMBAY",
      "status": "ACTIVE",
      "employeeNumber": "1443",
      ...
    }
  ]
}
```

### 2. Authorization Check (ตรวจสอบสิทธิ์)
```
POST https://open-api.dataslot.app/search/wfm/v1/{companyName}
```

### 3. Clock In/Out (บันทึกเวลา)
```
POST https://api.dataslot.app/wfm/{companyName}/tasks
```

---

## 🧪 ทดสอบ

### กรณีที่ 1: ผู้ใช้มี 1 บริษัท
1. Login ด้วย LINE
2. ระบบหาเจอ 1 บริษัท
3. เลือกอัตโนมัติ
4. ✅ เข้าสู่หน้าหลักทันที

### กรณีที่ 2: ผู้ใช้มีหลายบริษัท
1. Login ด้วย LINE
2. ระบบหาเจอ 3 บริษัท
3. แสดงหน้าเลือกบริษัท
4. ผู้ใช้เลือก "ATMBAY"
5. ✅ เข้าสู่หน้าหลักด้วยบริษัท ATMBAY

### กรณีที่ 3: ผู้ใช้ไม่มีบริษัท
1. Login ด้วย LINE
2. ระบบหาไม่เจอบริษัท
3. ✅ แสดงข้อความ "ไม่พบบริษัทในระบบ"

### กรณีที่ 4: Clock In
1. ผู้ใช้เลือกบริษัท ATMBAY แล้ว
2. กด Clock In
3. API เรียกไปที่ `/wfm/ATMBAY/tasks`
4. ✅ บันทึกเวลาสำเร็จ

---

## ⚠️ สิ่งที่ต้องทราบ

### สำหรับผู้ใช้เดิม
- ต้อง **login ใหม่** หลังอัพเดท
- ถ้ามีแค่ 1 บริษัท → ไม่มีอะไรเปลี่ยน (auto-select)
- ถ้ามีหลายบริษัท → จะเห็นหน้าเลือกบริษัทใหม่

### การเปลี่ยนบริษัท
- ปัจจุบัน: ต้อง **logout แล้ว login ใหม่**
- อนาคต: จะเพิ่มปุ่ม "เปลี่ยนบริษัท" ในหน้า Profile

### โหมด Offline
- การเลือกบริษัทต้องมีอินเทอร์เน็ต
- หลังเลือกแล้ว จะใช้บริษัทนั้นต่อแม้ offline

---

## 📋 Checklist

- [x] สร้าง service สำหรับจัดการบริษัท
- [x] สร้างหน้าเลือกบริษัท
- [x] แก้ไข API ให้เป็น dynamic
- [x] แก้ไข flow การ login
- [x] แสดงบริษัทในหน้า profile
- [x] อัพเดท README
- [x] ทดสอบทุกกรณี
- [x] ไม่มี linter errors

---

## 🎉 สรุป

แอป HR ตอนนี้รองรับการทำงานหลายบริษัทแล้ว! ผู้ใช้สามารถ:
- ✅ เข้าถึงข้อมูลหลายบริษัทได้
- ✅ เลือกบริษัทที่ต้องการใช้งาน
- ✅ Clock In/Out ในบริษัทที่เลือก
- ✅ ดูข้อมูลบริษัทปัจจุบันในหน้า Profile

**เวอร์ชัน**: 2.0.0  
**วันที่**: 17 พฤศจิกายน 2024  
**สถานะ**: ✅ เสร็จสมบูรณ์

